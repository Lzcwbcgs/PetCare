#   2024 年全国硕士研究生招生考试 数学（一）真题

## 一、选择题（1~10 小题，每小题 5 分，共 50 分） 下列每题给出的四个选项中，只有一个选项是符合题目要求的。

1. 已知函数 $f(x) = \int_{0}^{x} \mathrm{e}^{\cos t}\mathrm{d}t$，$g(x) = \int_{0}^{\sin x} \mathrm{e}^{t^2}\mathrm{d}t$，则$\underline{\quad\quad}$。

A. $f(x)$ 为奇函数，$g(x)$ 为偶函数
B. $f(x)$ 为偶函数，$g(x)$ 为奇函数
C. $f(x)$ 与 $g(x)$ 均为奇函数
D. $f(x)$ 与 $g(x)$ 均为周期函数

2. 设 $P = P(x,y,z),\ Q = Q(x,y,z)$ 均为连续函数，$\Sigma$ 为曲面 $z = \sqrt{1 - x^2 - y^2}\ (x \ge 0,\ y \ge 0)$ 的上侧，则
   $$
   \iint_{\Sigma} P\mathrm{d}y\mathrm{d}z + Q\mathrm{d}z\mathrm{d}x = \underline{\quad\quad}.
   $$

(A) $\iint_{\Sigma} \left( \frac{x}{z}P + \frac{y}{z}Q \right)\mathrm{d}x\mathrm{d}y$
(B) $\iint_{\Sigma} \left( -\frac{x}{z}P + \frac{y}{z}Q \right)\mathrm{d}x\mathrm{d}y$
(C) $\iint_{\Sigma} \left( \frac{x}{z}P - \frac{y}{z}Q \right)\mathrm{d}x\mathrm{d}y$
(D) $\iint_{\Sigma} \left( -\frac{x}{z}P - \frac{y}{z}Q \right)\mathrm{d}x\mathrm{d}y$

3. 已知幂级数 $\sum_{n=0}^{\infty} a_n x^n$ 的和函数为 $\ln(2 + x)$，则 $\sum_{n=0}^{\infty} n a_{2n} = \underline{\quad\quad}$。

A. $-\frac{1}{6}$
B. $-\frac{1}{3}$
C. $\frac{1}{6}$
D. $\frac{1}{3}$

4. 设函数 $f(x)$ 在区间 $(-1,1)$ 内有定义，且 $\lim_{x \to 0} f(x) = 0$，则$\underline{\quad\quad}$。

(A) 当 $\lim_{x \to 0} \frac{f(x)}{x} = m$ 时，$f'(0) = m$
(B) 当 $f'(0) = m$ 时，$\lim_{x \to 0} \frac{f(x)}{x} = m$
(C) 当 $\lim_{x \to 0} f'(x) = m$ 时，$f'(0) = m$
(D) 当 $f'(0) = m$ 时，$\lim_{x \to 0} f'(x) = m$

5. 在空间直角坐标系 $O-xyz$ 中，三张平面 $\pi_i: a_i x + b_i y + c_i z = d_i\ (i=1,2,3)$ 的位置关系如图所示，记

   $$
   \boldsymbol{\alpha}_i = (a_i,b_i,c_i),\quad \boldsymbol{\beta}_i = (a_i,b_i,c_i,d_i).
   $$

   若

   $$
      r\begin{pmatrix}
      \boldsymbol{\alpha}_1 \\
      \boldsymbol{\alpha}_2 \\
      \boldsymbol{\alpha}_3
      \end{pmatrix} = m,\quad
      r\begin{pmatrix}
      \boldsymbol{\beta}_1 \\
      \boldsymbol{\beta}_2 \\
      \boldsymbol{\beta}_3
      \end{pmatrix} = n,
   $$

   则$\underline{\quad\quad}$。<img src="2024-1-5.png" alt="alt text" style="zoom:10%;" />

A. $m = 1,\ n = 2$
B. $m = n = 2$
C. $m = 2,\ n = 3$
D. $m = n = 3$

6. 设向量
   $$
   \boldsymbol{\alpha}_1 = \begin{pmatrix} a \\ 1 \\ -1 \\ 1 \end{pmatrix},\quad
   \boldsymbol{\alpha}_2 = \begin{pmatrix} 1 \\ 1 \\ b \\ a \end{pmatrix},\quad
   \boldsymbol{\alpha}_3 = \begin{pmatrix} 1 \\ a \\ -1 \\ 1 \end{pmatrix}.
   $$
   若 $\boldsymbol{\alpha}_1,\boldsymbol{\alpha}_2,\boldsymbol{\alpha}_3$ 线性相关，且其中任意两个向量均线性无关，则$\underline{\quad\quad}$。

A. $a = 1,\ b \ne -1$
B. $a = 1,\ b = -1$
C. $a \ne -2,\ b = 2$
D. $a = -2,\ b = 2$

---

7. 设 $\boldsymbol{A}$ 是秩为 2 的 3 阶矩阵，$\boldsymbol{\alpha}$ 是满足 $\boldsymbol{A}\boldsymbol{\alpha} = \boldsymbol{0}$ 的非零向量。若对满足 $\boldsymbol{\beta}^\top\boldsymbol{\alpha} = 0$ 的 3 维列向量 $\boldsymbol{\beta}$，均有 $\boldsymbol{A}\boldsymbol{\beta} = \boldsymbol{\beta}$，则$\underline{\quad\quad}$。

A. $\boldsymbol{A}^3$ 的迹为 2
B. $\boldsymbol{A}^3$ 的迹为 5
C. $\boldsymbol{A}^2$ 的迹为 8
D. $\boldsymbol{A}^2$ 的迹为 9

---

8. 设随机变量 $X,Y$ 相互独立，且 $X$ 服从正态分布 $N(0,2)$，$Y$ 服从正态分布 $N(-2,2)$。若 $P\{2X + Y < a\} = P\{X > Y\}$，则 $a = \underline{\quad\quad}$。

A. $-2 - \sqrt{10}$
B. $-2 + \sqrt{10}$
C. $-2 - \sqrt{6}$
D. $-2 + \sqrt{6}$

---

9. 设随机变量 $X$ 的概率密度为
   $$
   f(x) =
   \begin{cases}
   2(1 - x), & 0 < x < 1, \\
   0, & \text{其他}.
   \end{cases}
   $$
   在 $X = x\ (0 < x < 1)$ 条件下，随机变量 $Y$ 服从区间 $(x,1)$ 上的均匀分布，则 $\mathrm{Cov}(X,Y) = \underline{\quad\quad}$。

A. $-\frac{1}{36}$
B. $-\frac{1}{72}$
C. $\frac{1}{72}$
D. $\frac{1}{36}$

---

10. 设随机变量 $X,Y$ 相互独立，且均服从参数为 $\lambda$ 的指数分布。令 $Z = |X - Y|$，则下列随机变量中与 $Z$ 同分布的是$\underline{\quad\quad}$。

A. $X + Y$
B. $\frac{X + Y}{2}$
C. $2X$
D. $X$

## 二、填空题（11~16 小题，每小题 5 分，共 30 分）

11. 若 $\lim_{x \to 0} \frac{(1 + ax^2)^{\sin x} - 1}{x^3} = 6$，则 $a = \underline{\quad\quad}$。

12. 设函数 $f(u,v)$ 具有 2 阶连续偏导数，且 $\mathrm{d}f\big|_{(1,1)} = 3\mathrm{d}u + 4\mathrm{d}v$。令 $y = f(\cos x, 1 + x^2)$，则 $\frac{\mathrm{d}^2 y}{\mathrm{d}x^2}\big|_{x=0} = \underline{\quad\quad}$。

13. 已知函数 $f(x) = x + 1$。若 $f(x) = \frac{a_0}{2} + \sum_{n=1}^{\infty} a_n \cos nx,\ x \in [0,\pi]$，则 $\lim_{n \to \infty} n^2 \sin a_{2n-1} = \underline{\quad\quad}$。

14. 微分方程 $y' = \frac{1}{(x + y)^2}$ 满足条件 $y(1) = 0$ 的解为 $\underline{\quad\quad}$。

15. 设实矩阵 $\boldsymbol{A} = \begin{pmatrix} a+1 & a \\ a & a \end{pmatrix}$。若对任意实向量 $\boldsymbol{\alpha} = \begin{pmatrix} x_1 \\ x_2 \end{pmatrix},\ \boldsymbol{\beta} = \begin{pmatrix} y_1 \\ y_2 \end{pmatrix}$，都有

    $$
    (\boldsymbol{\alpha}^\top \boldsymbol{A} \boldsymbol{\beta})^2 \le \boldsymbol{\alpha}^\top \boldsymbol{A} \boldsymbol{\alpha}\ \boldsymbol{\beta}^\top \boldsymbol{A} \boldsymbol{\beta}
    $$

    成立，则 $a$ 的取值范围是 $\underline{\quad\quad}$。

16. 设随机试验每次成功的概率为 $p$，现进行 3 次独立重复试验，在至少成功 1 次的条件下 3 次试验全部成功的概率为 $\frac{4}{13}$，则 $p = \underline{\quad\quad}$。

## 三、解答题（17~22 小题，共 70 分） 解答应写出文字说明、证明过程或演算步骤。

17. (本题满分 10 分)

已知平面区域 $D = \{(x,y) \mid \sqrt{1 - y^2} \le x \le 1,\ -1 \le y \le 1\}$，计算

$$
\iint_{D} \frac{x}{\sqrt{x^2 + y^2}}\mathrm{d}x\mathrm{d}y.
$$

---

18. (本题满分 12 分)

已知函数 $f(x,y) = x^3 + y^3 - (x + y)^2 + 3$，设 $\Gamma$ 是曲面 $z = f(x,y)$ 在点 $(1,1,1)$ 处的切平面，$D$ 为 $\Gamma$ 与坐标平面所围有界区域在 $xOy$ 平面上的投影。

(1) 求 $\Gamma$ 的方程；

(2) 求 $f(x,y)$ 在 $D$ 上的最大值和最小值。

---

19. (本题满分 12 分)

设函数 $f(x)$ 具有 2 阶导数，且 $f'(0) = f'(1) = 0$，$|f''(x)| \le 1$。证明：

(1) 当 $x \in (0,1)$ 时，$|f(x) - f(0)(1 - x) - f(1)x| \le \frac{x(1 - x)}{2}$；

(2) $\left| \int_{0}^{1} f(x)\mathrm{d}x - \frac{f(0) + f(1)}{2} \right| \le \frac{1}{12}$。

---

20. (本题满分 12 分)

已知有向曲线 $L$ 为球面 $x^2 + y^2 + z^2 = 2x$ 与平面 $2x - z - 1 = 0$ 的交线，从 $z$ 轴正向往 $z$ 轴负向看去为逆时针方向，计算曲线积分

$$
\int_{L} (6xyz - yz^2)\mathrm{d}x + 2x^2z\mathrm{d}y + xyz\mathrm{d}z.
$$

21. (本题满分 12 分)

已知数列 $\{x_n\},\{y_n\},\{z_n\}$ 满足 $x_0 = -1,\ y_0 = 0,\ z_0 = 2$，且

$$
\begin{cases}
x_n = -2x_{n-1} + 2z_{n-1}, \\
y_n = -2y_{n-1} - 2z_{n-1}, \\
z_n = -6x_{n-1} - 3y_{n-1} + 3z_{n-1}.
\end{cases}
$$

记 $\boldsymbol{\alpha}_n = \begin{pmatrix} x_n \\ y_n \\ z_n \end{pmatrix}$，写出满足 $\boldsymbol{\alpha}_n = \boldsymbol{A}\boldsymbol{\alpha}_{n-1}$ 的矩阵 $\boldsymbol{A}$，并求 $\boldsymbol{A}^n$ 及 $x_n,\ y_n,\ z_n\ (n = 1,2,\cdots)$。

---

22. (本题满分 12 分)

设总体 $X$ 服从 $[0,\theta]$ 上的均匀分布，其中 $\theta \in (0,+\infty)$ 为未知参数。$X_1,X_2,\cdots,X_n$ 是来自总体 $X$ 的简单随机样本，记 $X_{(n)} = \max\{X_1,X_2,\cdots,X_n\}$，$T_c = cX_{(n)}$。

(1) 求 $c$，使得 $T_c$ 是 $\theta$ 的无偏估计；

(2) 记 $h(c) = E(T_c - \theta)^2$，求 $c$ 使得 $h(c)$ 最小。
